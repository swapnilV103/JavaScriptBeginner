console.log("Hello World");

// varaibles are loosly coupled

var num = 9;
var strName = "Swapnil"

// along with var can also use let, const.  var and let are almost same but not all the way
// we cannot re-declare variable in let but can do it using var. Re-assign is allowed in both.
//const ! re-assign, re-declare
let a = 5;
console.log(typeof(a))

console.log(num+"  "+strName);